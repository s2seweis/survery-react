import React, { useEffect, useState } from 'react';

// ### - Comment to the integration
{/* <button>e1<button/>
onclick add Json data => push the data, to the object
the data that you push in the object should have a flexible reference out 
make it simple, start with the survey name, push it to the data object and go on from there 
const data = {} */}
{/* ### */}


const AdminAddSurvey = ({ match }) => {
  

  return (
    <div>

      <h1>Admin AddSurvey Area</h1>
   
    </div>
  );
}

export default AdminAddSurvey;